TITLE PROGRAM TO ADD TWO 16 BIT NUMBERS IN MEMORY
.MODEL SMALL
.STACK 32
.CODE
MAIN PROC FAR
        MOV BX, 0300H
        MOV AX, [BX]
        ADD AX, [BX + 2]
        MOV [BX + 4], AX
        MOV AH, 4CH
        INT 21H
 MAIN ENDP
 END
