TITLE PROGRAM TO ADD FOUR 16 BITS NUMS
.MODEL SMALL
.STACK 32
.CODE
MAIN PROC FAR
        MOV BX, 0200H
        MOV CX, 0004H
        MOV Ax, 0000H
   NEXT:
        ADD AX, [BX]
        ADD BX, 02H
        LOOP NEXT
        MOV [BX], AX
MAIN ENDP
END
