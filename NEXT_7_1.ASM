PAGE 30, 100
TITLE PROGRAM TO SUM FROM 00 TO 255
.MODEL SMALL
.STACK 32
.DATA
	SUM DW ?
.CODE
MAIN PROC FAR
	MOV AX, @DATA
	MOV DS, AX
	MOV CX, 00FFH
	MOV AX, 0000H
NEXT:	INC AX
	LOOP NEXT
	MOV SUM, AX
	MOV AX, 4C00H
	INT 21H

MAIN ENDP
END MAIN
